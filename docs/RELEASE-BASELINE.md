# 公开基线整理记录

日期：2026-09-17。

## 本次处理

- 从最新工作文件提取源码与测试，建立全新Git历史；没有搬运原项目历史。
- 排除运行配置、授权、数据库、日志、附件、私钥、客户报告和一次性客户运维脚本。
- 将源码中的原企业名称和测试人员/企业名称替换成示例值。
- 保留API、企微网关、Agent、业务规则、持久化和网页源码，不破坏原工作目录。
- 新增独立部署编排、镜像定义、配置样例、部署手册和GitHub测试工作流。
- 删除指向未公开Windows保活脚本的包命令；部署使用容器重启策略。
- 修正两项测试的旧文案断言和本机配置依赖，保留企业关联/重复确认不重复写入的行为验证。

## 本地验证

- `pnpm install --frozen-lockfile`：通过。
- `pnpm typecheck`：通过。
- `pnpm test`：55个文件、502项测试通过。
- 未复制运行数据；提交前检查文件清单及常见密钥/客户链接模式。

## 尚未验证

- 当前整理机器未安装Docker，未构建运行Linux容器。
- 未使用真实密钥调用模型，未向任何用户发送消息，未修改企微文档。
- 未连接或部署到新旧服务器。
- 未验收最新静态客户稿与动态H5功能一致性。

完整测试通过说明当前源码基线可继续开发，不代表企微全量文档存储、实际客户配置或生产安全验收已经完成。

## 确认模板补齐

- 补回 `templates/approved/manager-weekly.html`、`employee-weekly.html`，保留已确认的排版和交互，身份与新企业资料已替换为示例。
- 加入 `contract.json` 固定五类事项、卡片布局及关键交互约定。
- 动态老板端同步五分类与1+2+2布局；员工事项标题同步分类，展开保留原事项名称。异单位汇总按事项数，不混加原始数量；未知进度不显示为0。
- 静态模板通过320/390/1440尺寸、姓名跳转返回、事项弹窗和脚本异常检查；动态老板端用模拟事实做390宽度渲染检查。
- 新增模板契约及脚本校验，后续CI防止遗漏模板或分类漂移。
- 动态端与静态端剩余交互差距见模板README，本次不声称所有真实API链路已完成对齐。
